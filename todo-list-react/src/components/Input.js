import React from 'react';

const Input = () => {
    return (
        <div className="input">
        <form action="submit">
            <input type="text" placeholder="Add a task"/>
        </form>
            
        </div>
    );
};

export default Input;